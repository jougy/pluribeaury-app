---
const { status } = Astro.props;

const labelMap: Record<string, string> = {
	pendente: 'Pendente',
	confirmado: 'Confirmado',
	concluido: 'Concluído',
	cancelado: 'Cancelado',
};
---

<span class:list={['status-badge', `status-badge--${status}`]}>{labelMap[status] ?? status}</span>
