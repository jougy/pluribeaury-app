---
const { category } = Astro.props;
---

<article class="category-card" style={`--category-image: url('${category.image}');`}>
	<div class="category-card__content">
		<p class="eyebrow">Especialidade</p>
		<h3>{category.name}</h3>
		<p>{category.description}</p>
	</div>
</article>
